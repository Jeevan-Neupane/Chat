import { LoadingScreenDiv } from "./loadingscreenstyle";

const LoadingScreen = () => {
  return (
    <LoadingScreenDiv>
      <div className='loader'></div>
    </LoadingScreenDiv>
  );
};

export default LoadingScreen;
